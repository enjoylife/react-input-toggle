language: node_js
node_js:
  - "0.10"
before_script:
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start

before_install:
  - npm install -g npm@latest

install:
  - npm install -g codecov


after_success:
  - npm run cov

cache:
  directories:
    - node_modules
